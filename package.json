{
  "name": "package-tracker",
  "version": "1.0.0",
  "description": "Universal package tracking application - works as standalone web app or with backend server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gcloud run deploy package-tracker --source . --platform managed --region us-central1 --allow-unauthenticated"
  },
  "keywords": [
    "package",
    "tracking",
    "usps",
    "ups",
    "fedex",
    "dhl",
    "shipping"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
} 